@model ShoeShopOnline.WebApp.Models.ResetPasswordModel
@{
    ViewBag.Title = "ResetPassword";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<!--start-account-->
<div class="account">
    <div class="container">
        <div class="account-bottom">
            <div class="col-md-3"></div>
            <div class="col-md-6 account-left">
                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()
                    <form>
                        <div class="account-top heading">
                            <h3>Đặt lại mật khẩu</h3>
                        </div>
                        <div class="text-success">
                            @ViewBag.Message
                        </div>
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                        <div class="address">
                            <span>Mật khẩu</span>
                            @Html.EditorFor(model => model.NewPassword, new { })
                        </div>

                        <div class="address">
                            <span>Xác nhận mật khẩu</span>
                            @Html.EditorFor(model => model.ConfirmPassword, new { })
                        </div>
                        @Html.HiddenFor(a => a.ResetCode)

                        <div class="address">
                            <a class="" href="/dang-nhap">Đăng nhập</a>
                            <input type="submit" value="Xác nhận"/>
                        </div>
                    </form>
                }
                <div class="clearfix"> </div>
            </div>
            <div class="col-md-3"></div>
        </div>
    </div>
</div>
<!--end-account-->
<div>
    @Html.ActionLink("Trang chủ", "Index")
</div>

